# core
mcp>=1.0.0,<2
markitdown[all]
uvicorn  # P2 — SSE transport

# P1 — OCR (image PDFs)
pymupdf
pytesseract
pillow

# P1 — high-fidelity conversion (optional, heavy: ~2GB models on first run)
marker-pdf  # quality="accurate" backend — requires Python 3.13+ venv
